<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Student - SMS</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">
    <%- include('../partials/navbar') %>
    
    <main class="container mx-auto px-4 py-8">
        <div class="max-w-2xl mx-auto bg-white p-8 rounded-lg shadow-md">
            <div class="flex justify-between items-center mb-6">
                <h1 class="text-2xl font-bold">Edit Student</h1>
                <a href="/admin/dashboard" class="text-blue-500 hover:text-blue-700">
                    Back to Dashboard
                </a>
            </div>

            <% if (locals.error) { %>
                <div class="mb-4 p-4 bg-red-100 border border-red-400 text-red-700 rounded">
                    <%= error %>
                </div>
            <% } %>
            
            <form action="/admin/edit/<%= student.registrationNumber %>" method="POST" enctype="multipart/form-data" class="space-y-4">
                <div>
                    <label class="block text-gray-700 mb-2" for="registrationNumber">
                        Registration Number
                    </label>
                    <input type="text" id="registrationNumber" name="registrationNumber" 
                           value="<%= student.registrationNumber %>" required
                           class="w-full px-3 py-2 border rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
                </div>
                
                <div>
                    <label class="block text-gray-700 mb-2" for="rollNumber">
                        Roll Number
                    </label>
                    <input type="text" id="rollNumber" name="rollNumber" 
                           value="<%= student.rollNumber %>" required
                           class="w-full px-3 py-2 border rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
                </div>
                
                <div>
                    <label class="block text-gray-700 mb-2" for="registrationCard">
                        Registration Card Drive Link
                    </label>
                    <input type="url" id="registrationCard" name="registrationCard" 
                           value="<%= student.registrationCardPath %>"
                           placeholder="https://drive.google.com/file/d/..."
                           class="w-full px-3 py-2 border rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
                    <p class="text-sm text-gray-500 mt-1">Current link: <%= student.registrationCardPath %></p>
                </div>
                
                <div>
                    <label class="block text-gray-700 mb-2" for="resultCard">
                        Result Card Drive Link
                    </label>
                    <input type="url" id="resultCard" name="resultCard"
                           value="<%= student.resultCardPath %>"
                           placeholder="https://drive.google.com/file/d/..."
                           class="w-full px-3 py-2 border rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
                    <p class="text-sm text-gray-500 mt-1">Current link: <%= student.resultCardPath %></p>
                </div>
                
                <div class="flex justify-end space-x-4">
                    <a href="/admin/dashboard" 
                       class="px-6 py-2 border border-gray-300 rounded-md hover:bg-gray-50">
                        Cancel
                    </a>
                    <button type="submit" 
                            class="px-6 py-2 bg-blue-500 text-white rounded-md hover:bg-blue-600">
                        Update Student
                    </button>
                </div>
            </form>
        </div>
    </main>
    
    <%- include('../partials/footer') %>
</body>
</html>